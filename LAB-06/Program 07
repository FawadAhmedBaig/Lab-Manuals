#include <iostream>
using namespace std;
int main(){
	int a,sum =0;
	for(int i= 0 ; i<5 ; i++)
	{
		cout<<"Enter a number : ";
		cin>>a;
		if(a>0)
		sum += a;
		else
		break;
	}
	cout<<sum;

   return 0;
}
