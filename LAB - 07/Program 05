#include <iostream>
using namespace std;

int main(int argc, char** argv) {
int a, sum = 0;

for(int i=1 ; i<10000 ; i++)
{
	cin>>a;
    if(a<0)
    break;
    sum = sum + a;
}
    cout<<"The sum is : "<<sum;
	return 0;
}
