#include <iostream>
using namespace std;
int main ()
{
float a,b,c,d , exp ;
cout<<"Enter the value of a : ";
cin>>a;
cout<<"Enter the value of b : ";
cin>>b;
cout<<"Enter the value of c : ";
cin>>c;
cout<<"Enter the value of d : ";
cin>>d;
exp = a * b / (-c * 31 / 13) * d;
cout<<"a * b / (-c * 31 / 13) * d = "<<a * b / (-c * 31 / 13) * d;

return 0;
